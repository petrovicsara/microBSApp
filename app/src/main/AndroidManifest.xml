<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.microbsentryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.StartServiceActivity"></activity>
        <activity android:name=".view.InsertCustomerActivity" />
        <activity android:name=".view.ListWorkersActivity" />
        <activity android:name=".view.InsertWorkerActivity" />
        <activity android:name=".view.OptionsActivity" />
        <activity android:name=".view.LogInActivity" />
        <activity android:name=".view.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>